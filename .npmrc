# React 19 / Next 16 era: some deps (e.g. @hookform/resolvers) declare
# peerOptional ranges npm cannot satisfy in this tree. Keep installs
# deterministic for every agent by relaxing peer resolution project-wide.
legacy-peer-deps=true
